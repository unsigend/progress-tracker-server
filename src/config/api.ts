// configure api version
const apiRoot = "/api";
const apiVersion = "v1";

const apiConfig = {
    bookAPIRoot: `${apiRoot}/${apiVersion}/books`,
    userAPIRoot: `${apiRoot}/${apiVersion}/user`,
};

export default apiConfig;
